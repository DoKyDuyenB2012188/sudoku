To run the program:
1. make sure you have python: python --version => show version
2. download pygame => pip install pygame
3. edit the font addresses in the files (because at the time of saving it is the original address on the computer)
4. open main.py then run
happy playing :))